import React from "react";

const TdHash = ({ hash }) => {
    return (
        <th className="py-2 text-start font-inter text-sm px-4">
            {hash}
        </th>
    );
};

export default TdHash;
